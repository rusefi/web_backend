
<IfModule mod_alias.c>
Redirect /docs/wiki/  https://wiki.rusefi.com
</IfModule>

<IfModule mod_rewrite.c>
RewriteEngine On

RewriteRule docs/wiki$  /Home
RewriteRule docs/wiki/$ /Home

RewriteCond %{HTTPS} !=on
RewriteRule .* https://%{HTTP_HOST}%{REQUEST_URI} [R,L]
</IfModule>
